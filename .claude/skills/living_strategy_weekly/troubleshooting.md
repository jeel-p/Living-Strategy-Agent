# Troubleshooting

- **Unknown skill:** Use this repo as the project root, or run `claude --plugin-dir plugins/living-strategy-plugin`, or use **`/living_strategy_weekly`** (underscores) if your client loads `.claude/skills/` only.
- **Invalid tool parameters:** Two-turn paste; or `brief-input.md` file.
